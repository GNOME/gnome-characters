#!@GJS@ --module
imports.package.init({ name: "@appid@",
                       version: "@PACKAGE_VERSION@",
                       prefix: "@prefix@",
                       libdir: "@libdir@" });

const {main} = await import('resource:///@resource_base_path@/js/main.js');
main(ARGV);
